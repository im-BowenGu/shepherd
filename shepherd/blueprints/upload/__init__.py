from datetime import datetime;import errno,os,shutil,tempfile,zipfile;from flask import Blueprint,render_template,flash,redirect,url_for,request,abort,current_app;from shepherd.blueprints import run;import robot.reset as robot_reset;blueprint=Blueprint("upload",__name__,template_folder="templates");@blueprint.route("/"):def index():return render_template("upload/index.html",last_upload_time=None);@blueprint.route("/upload",methods=["POST"]):def upload():try:file=request.files["uploaded_file"];except KeyError:abort(400);if file.filename=="":flash("You didn't upload a file.","error");else:flash(f"Got a file with filename {file.filename}","debug");err=process_uploaded_file(file);if err:flash(err,"error");else:flash("Your file looks good!","success");if run.reaper_timer is not None:run.reaper_timer.cancel();run.reap(reason="new code upload");robot_reset.reset();run._reset_state();run._start_user_code(current_app);return redirect(url_for(".index"));def process_uploaded_file(file):if file.mimetype.startswith("text") or file.filename.endswith(".py"):tempdir=tempfile.mkdtemp(prefix="shepherd-user-code-");file.save(os.path.join(tempdir,current_app.config["SHEPHERD_USER_CODE_ENTRYPOINT_NAME"]));shutil.rmtree(current_app.config["SHEPHERD_USER_CODE_PATH"]);shutil.move(tempdir,current_app.config["SHEPHERD_USER_CODE_PATH"]);return None;elif("zip"in file.mimetype or file.filename.endswith(".zip"))and zipfile.is_zipfile(file):tempdir=tempfile.mkdtemp(prefix="shepherd-user-code-");try:with zipfile.ZipFile(file,"r")as zip:zip.extractall(tempdir);except zipfile.BadZipfile:shutil.rmtree(tempdir);return"Your file looked like a zip file, but it isn't actually a valid zip file!";try:with open(os.path.join(tempdir,current_app.config["SHEPHERD_USER_CODE_ENTRYPOINT_NAME"]),"r"):pass;except IOError as e:shutil.rmtree(tempdir);if e.errno in(errno.ENOENT,errno.EISDIR):return"Your file is a zip file, but it doesn't include a main.py file!";else:return f"Your file is a zip file, but something went wrong after extracting it! (error: {errno.errorcode[e.errno]})";shutil.rmtree(current_app.config["SHEPHERD_USER_CODE_PATH"]);shutil.move(tempdir,current_app.config["SHEPHERD_USER_CODE_PATH"]);return None;else:return"Your file doesn't look like valid code. Make sure the extension is correct."
